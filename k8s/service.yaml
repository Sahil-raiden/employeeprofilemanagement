apiVersion: v1
kind: Service
metadata:
  name: employeeprofilemanagement-service
  namespace: employeeprofilemanagement-ns

spec:
  type: NodePort
  selector:
    app: employeeprofilemanagement

  ports:
  - port: 8200        # cluster port
    targetPort: 8200  # container port
    nodePort: 30082   # external port
